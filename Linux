
id = identificação
$ = usario comum
root rakear pode fazer tudo no linux
Ctrl + L = limpar
Crtl + Alt = t terminal
Crtl + D exit sair do root
sudo apt update = atualiza as lisas do apt (aplicativos)
sudo apt upgrade = atualiza todos os software 
sudo apt full-upgrade = atualiza todos os software (atualização completa)
sudo apt dist -upgrade = atualiza o sistema operacional
Kernel
sudo apt autoremove = remoção e software não mais necessária 
sudo apt autoclean = fazer a limpeza do sistema
sudo reboot = reiniciar
sudo apt install psensor = instalar aplicativos
sudo apt install p7zip-full p7zip-rar guvcview cheese 
sudo apt install kazam audacity vlc kolourpaint notepadqq
sudo init 0 = desliga


  
